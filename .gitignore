# Dependências — nunca versionar a pasta node_modules
node_modules/
app/frontend/node_modules/
app/backend/node_modules/

# Variáveis de ambiente — nunca versionar arquivos .env com senhas reais
.env
app/frontend/.env
app/backend/.env

# Arquivos de build do frontend
app/frontend/dist/

# Logs e arquivos temporários gerados pelo agente
*.log
migrate_output.txt
seed_output.txt
dev.sqlite3

# Scripts de scaffold e build (utilitários dos agentes, não fazem parte do produto)
buildMigrations.js
scaffold.js
build_backend.js
build_frontend_core.js
build_paciente_pages.cjs
build_gestor_and_remaining_pages.cjs

# Assets brutos do Stitch (imagens brutas — telas organizadas estão em /screens)
stitch_gest_o_sa_de_ubs/

# Configurações locais do Claude Code (settings pessoais, tokens de MCP)
.claude/
.mcp.json

# Sistema operacional
.DS_Store
Thumbs.db

# Scripts temporários de validação e tarefas com credenciais
app/backend/validar_banco.js
TASK_07_infra_vercel.md

# Skills de agentes (não fazem parte do produto — gerenciadas externamente)
.agents/

# Build do hub de mockups na raiz
dist/
